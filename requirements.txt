# ============================================================
# Dependencias del proyecto — Sistema de Recomendación de Películas
# Instalar con: pip install -r requirements.txt
# ============================================================
 
neo4j==5.20.0          # Driver oficial de Neo4j para Python
python-dotenv==1.0.1   # Carga de variables de entorno desde .env
colorama==0.4.6        # Colores en la terminal (CLI amigable)
tabulate==0.9.0        # Formateo de tablas en consola
 
